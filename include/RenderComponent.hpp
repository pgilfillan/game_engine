#pragma once

class Mesh;

class RenderComponent {

public:/*
    RenderComponent(Mesh m, Texture t, Shader s, Material m): _m(m) {}
    std::unique_ptr<Mesh>& getMesh() {return _m;} 

private:
    std::unique_ptr<Mesh> _m;
*/
}